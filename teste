<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>7 Anos Escolhendo Você | Stéfano & Amanda</title>
<meta name="description" content="Uma carta de amor, gratidão e propósito celebrando 7 anos de história entre Stéfano e Amanda.">

<!-- ===================== GOOGLE FONTS ===================== -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,400;1,500&family=Jost:wght@200;300;400;500;600&display=swap" rel="stylesheet">

<style>
  /* =========================================================
     DESIGN TOKENS
     -----------------------------------------------------------
     Cor:
       --cream:      #FBF7F0  (fundo base)
       --ivory:      #FFFFFF  (cartões / contraste)
       --sand:       #EFE6D8  (seções alternadas)
       --gold:       #C9A24B  (acento dourado suave)
       --gold-soft:  #E8D9B5  (acento dourado claro)
       --ink:        #2E2A24  (texto principal)
       --ink-soft:   #6E6557  (texto secundário)
       --contrast:   #3A2E22  (seção de propósito - marrom profundo)

     Tipografia:
       Display / romântico: 'Cormorant Garamond' (itálico para frases-chave)
       Suporte / legibilidade: 'Jost' (leve, geométrica, moderna)
  ========================================================= */

  :root{
    --cream: #FBF7F0;
    --ivory: #FFFFFF;
    --sand: #F1E8DA;
    --gold: #C9A24B;
    --gold-soft: #E8D9B5;
    --ink: #2E2A24;
    --ink-soft: #8A7F6E;
    --contrast: #352A20;
    --contrast-text: #F3E9D8;

    --ease: cubic-bezier(.16,.8,.24,1);
  }

  *{ margin:0; padding:0; box-sizing:border-box; }

  html{ scroll-behavior:smooth; }

  body{
    font-family:'Jost', sans-serif;
    font-weight:300;
    color:var(--ink);
    background:var(--cream);
    overflow-x:hidden;
    line-height:1.7;
  }

  img{ display:block; max-width:100%; }

  .serif{
    font-family:'Cormorant Garamond', serif;
  }

  /* =========================================================
     UTILITIES
  ========================================================= */
  .container{
    max-width:1200px;
    margin:0 auto;
    padding:0 24px;
  }

  .section{
    position:relative;
    padding:120px 0;
  }

  @media (max-width:768px){
    .section{ padding:80px 0; }
  }

  .eyebrow{
    font-family:'Jost', sans-serif;
    font-size:.72rem;
    letter-spacing:.32em;
    text-transform:uppercase;
    color:var(--gold);
    font-weight:500;
    margin-bottom:18px;
    display:inline-block;
  }

  .reveal{
    opacity:0;
    transform:translateY(36px);
    transition:opacity 1.1s var(--ease), transform 1.1s var(--ease);
    will-change:opacity, transform;
  }
  .reveal.is-visible{
    opacity:1;
    transform:translateY(0);
  }
  .reveal-zoom{
    opacity:0;
    transform:scale(1.06);
    transition:opacity 1.3s var(--ease), transform 1.6s var(--ease);
  }
  .reveal-zoom.is-visible{
    opacity:1;
    transform:scale(1);
  }

  @media (prefers-reduced-motion: reduce){
    .reveal, .reveal-zoom, .float-img, .particles span{
      transition:none !important;
      animation:none !important;
      opacity:1 !important;
      transform:none !important;
    }
  }

  /* =========================================================
     PHOTO PLACEHOLDER SYSTEM
     -----------------------------------------------------------
     Substitua trocando o atributo "src" das tags <img>
     e/ou os data-bg em divs com classe .ph (placeholder).
     Cada .ph mantém proporção via aspect-ratio para já
     reservar o layout antes das fotos chegarem.
  ========================================================= */
  .ph{
    position:relative;
    width:100%;
    overflow:hidden;
    background:
      linear-gradient(135deg, var(--sand) 0%, var(--gold-soft) 100%);
    display:flex;
    align-items:center;
    justify-content:center;
    color:var(--ink-soft);
  }
  .ph img{
    position:absolute;
    inset:0;
    width:100%;
    height:100%;
    object-fit:cover;
    opacity:0;
    transition:opacity 1s var(--ease), transform 8s ease-out;
  }
  .ph img.loaded{ opacity:1; }
  .ph .ph-label{
    font-family:'Jost', sans-serif;
    font-size:.68rem;
    letter-spacing:.22em;
    text-transform:uppercase;
    text-align:center;
    padding:18px;
    z-index:1;
    pointer-events:none;
  }
  .ph .ph-label small{
    display:block;
    font-size:.62rem;
    letter-spacing:.1em;
    opacity:.7;
    margin-top:6px;
    font-family:'Cormorant Garamond', serif;
    font-style:italic;
  }

  /* gentle ambient zoom on hero / fullscreen images */
  .ph.zoom-slow img{
    transform:scale(1.08);
    transition:opacity 1.2s var(--ease), transform 14s ease-out;
  }
  .ph.zoom-slow img.loaded{
    transform:scale(1);
  }

  /* =========================================================
     NAVIGATION (minimal, fades in on scroll)
  ========================================================= */
  .nav{
    position:fixed;
    top:0; left:0; right:0;
    z-index:100;
    padding:28px 0;
    display:flex;
    justify-content:center;
    pointer-events:none;
    opacity:0;
    transition:opacity .6s ease, padding .4s ease;
    mix-blend-mode:difference;
  }
  .nav.visible{ opacity:1; }
  .nav .nav-inner{
    font-family:'Cormorant Garamond', serif;
    font-style:italic;
    font-size:1.1rem;
    letter-spacing:.05em;
    color:#fff;
  }

  /* =========================================================
     SECTION 1 — HERO
  ========================================================= */
  .hero{
    position:relative;
    height:100vh;
    min-height:640px;
    width:100%;
    overflow:hidden;
    display:flex;
    align-items:flex-end;
    justify-content:center;
  }
  .hero .ph{
    position:absolute;
    inset:0;
    height:100%;
    border-radius:0;
  }
  .hero::after{
    content:"";
    position:absolute;
    inset:0;
    background:linear-gradient(
      to bottom,
      rgba(20,15,8,.05) 0%,
      rgba(20,15,8,.15) 45%,
      rgba(20,15,8,.62) 100%
    );
    z-index:2;
    pointer-events:none;
  }

  /* floating soft particles */
  .particles{
    position:absolute;
    inset:0;
    z-index:3;
    pointer-events:none;
    overflow:hidden;
  }
  .particles span{
    position:absolute;
    bottom:-10%;
    display:block;
    width:6px;
    height:6px;
    border-radius:50%;
    background:radial-gradient(circle, #fff7e6 0%, rgba(255,247,230,0) 70%);
    opacity:.55;
    animation:floatUp linear infinite;
  }
  @keyframes floatUp{
    0%{ transform:translateY(0) translateX(0) scale(1); opacity:0; }
    10%{ opacity:.6; }
    50%{ transform:translateY(-50vh) translateX(20px) scale(1.4); }
    90%{ opacity:.4; }
    100%{ transform:translateY(-105vh) translateX(-10px) scale(.6); opacity:0; }
  }

  .hero-content{
    position:relative;
    z-index:4;
    width:100%;
    text-align:center;
    padding:0 24px 90px;
    color:#fff;
  }
  .hero-title{
    font-family:'Cormorant Garamond', serif;
    font-style:italic;
    font-weight:500;
    font-size:clamp(2.6rem, 8vw, 5.5rem);
    line-height:1.08;
    letter-spacing:.01em;
    text-shadow:0 2px 30px rgba(0,0,0,.25);
  }
  .hero-sub{
    max-width:620px;
    margin:26px auto 38px;
    font-size:clamp(.95rem, 1.6vw, 1.12rem);
    font-weight:300;
    color:rgba(255,255,255,.92);
    letter-spacing:.01em;
  }
  .btn{
    display:inline-flex;
    align-items:center;
    gap:12px;
    font-family:'Jost', sans-serif;
    font-size:.78rem;
    letter-spacing:.28em;
    text-transform:uppercase;
    font-weight:500;
    color:#fff;
    text-decoration:none;
    padding:18px 38px;
    border:1px solid rgba(255,255,255,.55);
    border-radius:999px;
    transition:background .45s var(--ease), color .45s var(--ease), border-color .45s var(--ease), letter-spacing .45s var(--ease);
    backdrop-filter:blur(6px);
  }
  .btn:hover{
    background:var(--gold-soft);
    color:var(--contrast);
    border-color:var(--gold-soft);
    letter-spacing:.34em;
  }
  .btn svg{ transition:transform .45s var(--ease); }
  .btn:hover svg{ transform:translateY(3px); }

  .scroll-cue{
    position:absolute;
    bottom:28px; left:50%;
    transform:translateX(-50%);
    z-index:4;
    color:rgba(255,255,255,.7);
    font-size:.65rem;
    letter-spacing:.3em;
    text-transform:uppercase;
    display:flex;
    flex-direction:column;
    align-items:center;
    gap:10px;
  }
  .scroll-cue .line{
    width:1px; height:38px;
    background:linear-gradient(to bottom, rgba(255,255,255,.8), transparent);
    animation:cueMove 2.4s ease-in-out infinite;
  }
  @keyframes cueMove{
    0%,100%{ transform:scaleY(1); opacity:.6; }
    50%{ transform:scaleY(.55); opacity:1; }
  }

  /* =========================================================
     SECTION HEADERS (shared)
  ========================================================= */
  .section-head{
    text-align:center;
    max-width:680px;
    margin:0 auto 64px;
    padding:0 24px;
  }
  .section-head h2{
    font-family:'Cormorant Garamond', serif;
    font-style:italic;
    font-weight:500;
    font-size:clamp(2.1rem, 5vw, 3.4rem);
    color:var(--ink);
    line-height:1.15;
  }
  .section-head p{
    margin-top:18px;
    font-size:1rem;
    color:var(--ink-soft);
    font-weight:300;
  }
  .on-dark .section-head h2{ color:var(--contrast-text); }
  .on-dark .section-head p{ color:#d8c8ad; }
  .on-dark .eyebrow{ color:var(--gold-soft); }

  /* =========================================================
     SECTION 2 — TIMELINE (horizontal scroll gallery)
  ========================================================= */
  .timeline-track{
    display:flex;
    gap:24px;
    overflow-x:auto;
    padding:8px 24px 28px;
    scroll-snap-type:x proximity;
    scrollbar-width:none;
  }
  .timeline-track::-webkit-scrollbar{ display:none; }

  .timeline-card{
    flex:0 0 auto;
    scroll-snap-align:center;
    width:min(78vw, 360px);
  }
  .timeline-card .ph{
    aspect-ratio:4/5;
    border-radius:18px;
  }
  .timeline-card .cap{
    margin-top:16px;
    font-family:'Cormorant Garamond', serif;
    font-style:italic;
    font-size:1.05rem;
    color:var(--ink-soft);
    text-align:center;
  }

  .timeline-hint{
    text-align:center;
    margin-top:8px;
    font-size:.7rem;
    letter-spacing:.25em;
    text-transform:uppercase;
    color:var(--ink-soft);
    opacity:.6;
  }

  /* =========================================================
     SECTION 3 — LOVE LETTER
  ========================================================= */
  .letter-wrap{
    max-width:760px;
    margin:0 auto;
    padding:80px 40px;
    background:var(--ivory);
    border-radius:28px;
    box-shadow:0 30px 80px -40px rgba(60,46,20,.25);
    position:relative;
  }
  .letter-wrap::before{
    content:"";
    position:absolute;
    top:32px; left:32px; right:32px; bottom:32px;
    border:1px solid var(--gold-soft);
    border-radius:18px;
    pointer-events:none;
  }
  .letter-body{
    font-family:'Cormorant Garamond', serif;
    font-size:clamp(1.15rem, 2.4vw, 1.45rem);
    line-height:1.85;
    color:var(--ink);
  }
  .letter-body p{ margin-bottom:1.3em; }
  .letter-body .salut{ font-style:italic; font-size:1.15em; }
  .letter-signature{
    margin-top:46px;
    text-align:right;
    font-family:'Cormorant Garamond', serif;
    font-style:italic;
    font-size:1.6rem;
    color:var(--gold);
  }

  /* =========================================================
     SECTION 4 — CINEMATIC GALLERY GRID
  ========================================================= */
  .grid-gallery{
    display:grid;
    grid-template-columns:repeat(6, 1fr);
    gap:14px;
    padding:0 24px;
  }
  .grid-gallery .ph{
    border-radius:14px;
    cursor:pointer;
    transition:transform .6s var(--ease);
  }
  .grid-gallery .ph:hover{ transform:scale(1.015); }
  .grid-gallery .ph img{ transition:opacity 1s var(--ease), transform 1.2s var(--ease); }
  .grid-gallery .ph:hover img.loaded{ transform:scale(1.05); }

  .gg-1{ grid-column:span 4; aspect-ratio:16/10; }
  .gg-2{ grid-column:span 2; aspect-ratio:3/4; grid-row:span 2; }
  .gg-3{ grid-column:span 2; aspect-ratio:1/1; }
  .gg-4{ grid-column:span 2; aspect-ratio:1/1; }
  .gg-5{ grid-column:span 3; aspect-ratio:4/3; }
  .gg-6{ grid-column:span 3; aspect-ratio:4/3; }

  @media (max-width:860px){
    .grid-gallery{ grid-template-columns:repeat(2,1fr); }
    .gg-1{ grid-column:span 2; aspect-ratio:4/3; }
    .gg-2{ grid-column:span 1; grid-row:auto; aspect-ratio:3/4; }
    .gg-3,.gg-4{ grid-column:span 1; aspect-ratio:1/1; }
    .gg-5,.gg-6{ grid-column:span 1; aspect-ratio:1/1; }
  }

  /* lightbox modal */
  .lightbox{
    position:fixed; inset:0;
    background:rgba(28,21,14,.92);
    display:flex; align-items:center; justify-content:center;
    z-index:1000;
    opacity:0; visibility:hidden;
    transition:opacity .4s var(--ease), visibility .4s var(--ease);
    padding:32px;
  }
  .lightbox.open{ opacity:1; visibility:visible; }
  .lightbox img{
    max-width:min(92vw, 1100px);
    max-height:88vh;
    border-radius:10px;
    transform:scale(.96);
    transition:transform .4s var(--ease);
    object-fit:contain;
  }
  .lightbox.open img{ transform:scale(1); }
  .lightbox-close{
    position:absolute; top:28px; right:32px;
    color:#fff; font-family:'Jost',sans-serif;
    font-size:.75rem; letter-spacing:.25em; text-transform:uppercase;
    cursor:pointer; opacity:.75; transition:opacity .3s;
    background:none; border:none;
  }
  .lightbox-close:hover{ opacity:1; }

  /* =========================================================
     SECTION 5 — PURPOSE (contrast / dark)
  ========================================================= */
  .purpose{
    background:var(--contrast);
    color:var(--contrast-text);
    position:relative;
    overflow:hidden;
  }
  .purpose::before{
    content:"";
    position:absolute;
    inset:0;
    background:
      radial-gradient(ellipse at 20% 20%, rgba(201,162,75,.14), transparent 55%),
      radial-gradient(ellipse at 80% 85%, rgba(201,162,75,.10), transparent 55%);
    pointer-events:none;
  }
  .purpose-verse{
    text-align:center;
    position:relative;
    z-index:1;
    padding:0 24px;
    max-width:760px;
    margin:0 auto 64px;
  }
  .purpose-verse h2{
    font-family:'Cormorant Garamond', serif;
    font-style:italic;
    font-weight:500;
    font-size:clamp(2.4rem,6vw,4rem);
    line-height:1.2;
  }
  .purpose-verse .ref{
    display:block;
    margin-top:14px;
    font-family:'Jost', sans-serif;
    font-size:.72rem;
    letter-spacing:.32em;
    text-transform:uppercase;
    color:var(--gold-soft);
  }
  .purpose-verse .comp{
    margin-top:38px;
    font-size:1.05rem;
    line-height:1.9;
    color:#e6d9c2;
    font-weight:300;
  }

  .purpose-gallery{
    position:relative; z-index:1;
    display:grid;
    grid-template-columns:repeat(3,1fr);
    gap:14px;
    padding:0 24px;
  }
  .purpose-gallery .ph{ border-radius:14px; aspect-ratio:3/4; }
  @media (max-width:768px){
    .purpose-gallery{ grid-template-columns:repeat(2,1fr); }
    .purpose-gallery .ph:nth-child(3){ display:none; }
  }

  /* =========================================================
     SECTION 6 — COUNTER
  ========================================================= */
  .counter-section{
    text-align:center;
  }
  .counter-grid{
    display:flex;
    justify-content:center;
    gap:clamp(24px, 6vw, 90px);
    flex-wrap:wrap;
    margin-top:50px;
  }
  .counter-item{
    display:flex;
    flex-direction:column;
    align-items:center;
    gap:10px;
  }
  .counter-num{
    font-family:'Cormorant Garamond', serif;
    font-style:italic;
    font-weight:600;
    font-size:clamp(2.8rem, 9vw, 5.4rem);
    color:var(--gold);
    line-height:1;
    letter-spacing:.02em;
  }
  .counter-label{
    font-size:.72rem;
    letter-spacing:.3em;
    text-transform:uppercase;
    color:var(--ink-soft);
  }
  .counter-since{
    margin-top:44px;
    font-family:'Cormorant Garamond', serif;
    font-style:italic;
    font-size:1.05rem;
    color:var(--ink-soft);
  }

  /* =========================================================
     SECTION 7 — FINAL DECLARATION (fullscreen)
  ========================================================= */
  .final{
    position:relative;
    height:100vh;
    min-height:600px;
    overflow:hidden;
    display:flex;
    align-items:center;
    justify-content:center;
  }
  .final .ph{
    position:absolute; inset:0; height:100%;
    border-radius:0;
  }
  .final::after{
    content:"";
    position:absolute; inset:0;
    background:linear-gradient(to top, rgba(20,15,8,.7) 0%, rgba(20,15,8,.25) 55%, rgba(20,15,8,.45) 100%);
    z-index:2;
  }
  .final-content{
    position:relative; z-index:3;
    text-align:center;
    color:#fff;
    padding:0 24px;
  }
  .final-content h2{
    font-family:'Cormorant Garamond', serif;
    font-style:italic;
    font-weight:500;
    font-size:clamp(2.4rem, 7vw, 4.6rem);
    line-height:1.15;
  }
  .final-content .small{
    margin-top:22px;
    font-size:clamp(1rem, 2.2vw, 1.3rem);
    font-weight:300;
    color:rgba(255,255,255,.92);
    line-height:1.6;
  }
  .final-symbol{
    margin-top:34px;
    display:flex;
    align-items:center;
    justify-content:center;
    gap:18px;
    opacity:.85;
  }
  .final-symbol svg{ width:30px; height:30px; }
  .final-symbol .div{
    width:1px; height:24px;
    background:rgba(255,255,255,.4);
  }

  /* =========================================================
     FOOTER
  ========================================================= */
  footer{
    background:var(--sand);
    text-align:center;
    padding:70px 24px;
  }
  footer .verse{
    font-family:'Cormorant Garamond', serif;
    font-style:italic;
    font-size:clamp(1.3rem,3vw,1.8rem);
    color:var(--ink);
  }
  footer .ref{
    display:block;
    margin-top:10px;
    font-size:.68rem;
    letter-spacing:.3em;
    text-transform:uppercase;
    color:var(--ink-soft);
  }
  footer .names{
    margin-top:36px;
    font-family:'Jost', sans-serif;
    font-size:.85rem;
    letter-spacing:.25em;
    text-transform:uppercase;
    color:var(--gold);
  }
  footer .made{
    margin-top:18px;
    font-size:.68rem;
    color:var(--ink-soft);
    letter-spacing:.05em;
  }

</style>
</head>
<body>

<!-- =========================================================
     NAV (minimal monogram, fades in once hero is scrolled past)
========================================================= -->
<nav class="nav" id="nav">
  <div class="nav-inner">S &amp; A — 7 anos</div>
</nav>

<!-- =========================================================
     SEÇÃO 1 — HERO
========================================================= -->
<section class="hero" id="hero">

  <!-- PHOTO SLOT: foto principal do casal (retrato vertical ou paisagem ampla) -->
  <div class="ph zoom-slow" data-photo-slot="hero-main">
    <img data-src="fotos/04.jpeg" alt="Stéfano e Amanda" loading="eager">
    <div class="ph-label">FOTO PRINCIPAL DO CASAL<small>Substituir data-src — recomendado 1920×1280 ou maior</small></div>
  </div>

  <div class="particles" id="particles"></div>

  <div class="hero-content reveal" id="hero-text">
    <h1 class="hero-title">7 anos escolhendo você.</h1>
    <p class="hero-sub">Entre todos os caminhos que eu poderia seguir, Deus me conduziu até você. E todos os dias, há 7 anos, eu continuo fazendo a mesma escolha: te amar.</p>
    <a href="#timeline" class="btn">
      Nossa História
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 4v16M5 13l7 7 7-7"/></svg>
    </a>
  </div>

  <div class="scroll-cue">
    <span>role</span>
    <span class="line"></span>
  </div>
</section>

<!-- =========================================================
     SEÇÃO 2 — LINHA DO TEMPO VISUAL
========================================================= -->
<section class="section" id="timeline">
  <div class="section-head reveal">
    <span class="eyebrow">Linha do tempo</span>
    <h2>Capítulos que Deus escreveu</h2>
    <p>Cada momento, cada desafio, cada vitória e cada sorriso fizeram parte da história que o Senhor estava construindo.</p>
  </div>

  <div class="timeline-track reveal">
    <!-- PHOTO SLOTS: linha do tempo — adicione/remova cards conforme a quantidade de fotos -->
    <div class="timeline-card">
      <div class="ph" data-photo-slot="timeline-1">
        <img data-src="fotos/01.jpeg" alt="" loading="lazy">
        <div class="ph-label">CAPÍTULO 01<small>O começo</small></div>
      </div>
      <p class="cap">Onde tudo começou</p>
    </div>
    <div class="timeline-card">
      <div class="ph" data-photo-slot="timeline-2">
        <img data-src="fotos/03.png" alt="" loading="lazy">
        <div class="ph-label">CAPÍTULO 02<small>Os primeiros sonhos</small></div>
      </div>
      <p class="cap">Os primeiros sonhos juntos</p>
    </div>
    <div class="timeline-card">
      <div class="ph" data-photo-slot="timeline-3">
        <img data-src="fotos/02.jpeg" alt="" loading="lazy">
        <div class="ph-label">CAPÍTULO 03<small>Crescendo juntos</small></div>
      </div>
      <p class="cap">Crescendo em fé e amor</p>
    </div>
    <div class="timeline-card">
      <div class="ph" data-photo-slot="timeline-4">
        <img data-src="fotos/05.jpeg" alt="" loading="lazy">
        <div class="ph-label">CAPÍTULO 04<small>Desafios superados</small></div>
      </div>
      <p class="cap">Desafios que nos uniram</p>
    </div>
    <div class="timeline-card">
      <div class="ph" data-photo-slot="timeline-5">
        <img data-src="fotos/12.jpeg" alt="" loading="lazy">
        <div class="ph-label">CAPÍTULO 05<small>Hoje</small></div>
      </div>
      <p class="cap">E hoje, mais perto do propósito</p>
    </div>
  </div>
  <p class="timeline-hint">arraste para o lado →</p>
</section>

<!-- =========================================================
     SEÇÃO 3 — CARTA DE AMOR
========================================================= -->
<section class="section" id="letter">
  <div class="container">
    <div class="letter-wrap reveal">
      <div class="letter-body">
        <p class="salut">Amanda,</p>
        <p>Há 7 anos eu escolhi te amar.</p>
        <p>E o mais bonito é perceber que essa escolha nunca precisou ser refeita. Ela apenas cresceu.</p>
        <p>Deus tem sido testemunha de cada oração, de cada sonho compartilhado, de cada desafio superado e de cada momento em que caminhamos lado a lado.</p>
        <p>Você é uma das maiores demonstrações da bondade de Deus na minha vida.</p>
        <p>Obrigado por ser minha companheira, minha amiga, minha incentivadora e minha parceira de propósito.</p>
        <p>E se hoje eu pudesse fazer apenas uma promessa, seria a mesma de todos esses anos:</p>
        <p class="salut">Continuarei escolhendo você.<br>Hoje.<br>Amanhã.<br>E por toda a eternidade.</p>
      </div>
      <p class="letter-signature">Com amor, Stéfano</p>
    </div>
  </div>
</section>

<!-- =========================================================
     SEÇÃO 4 — GALERIA CINEMATOGRÁFICA
========================================================= -->
<section class="section" id="gallery">
  <div class="section-head reveal">
    <span class="eyebrow">Galeria</span>
    <h2>Nossos melhores momentos</h2>
  </div>

  <div class="grid-gallery reveal">
    <!-- PHOTO SLOTS: galeria principal — 6 espaços, proporções variadas para efeito de mosaico -->
    <div class="ph gg-1" data-photo-slot="gallery-1" data-lightbox>
      <img data-src="fotos/07.jpeg" alt="" loading="lazy">
      <div class="ph-label">FOTO 01 — DESTAQUE<small>paisagem, 16:10</small></div>
    </div>
    <div class="ph gg-2" data-photo-slot="gallery-2" data-lightbox>
      <img data-src="fotos/08.jpeg" alt="" loading="lazy">
      <div class="ph-label">FOTO 02<small>retrato, 3:4</small></div>
    </div>
    <div class="ph gg-3" data-photo-slot="gallery-3" data-lightbox>
      <img data-src="fotos/06.jpeg" alt="" loading="lazy">
      <div class="ph-label">FOTO 03<small>quadrada, 1:1</small></div>
    </div>
    <div class="ph gg-4" data-photo-slot="gallery-4" data-lightbox>
      <img data-src="fotos/03.png" alt="" loading="lazy">
      <div class="ph-label">FOTO 04<small>quadrada, 1:1</small></div>
    </div>
    <div class="ph gg-5" data-photo-slot="gallery-5" data-lightbox>
      <img data-src="fotos/02.jpeg" alt="" loading="lazy">
      <div class="ph-label">FOTO 05<small>paisagem, 4:3</small></div>
    </div>
    <div class="ph gg-6" data-photo-slot="gallery-6" data-lightbox>
      <img data-src="fotos/05.jpeg" alt="" loading="lazy">
      <div class="ph-label">FOTO 06<small>paisagem, 4:3</small></div>
    </div>
  </div>
</section>

<!-- =========================================================
     SEÇÃO 5 — PROPÓSITO
========================================================= -->
<section class="section purpose on-dark" id="purpose">
  <div class="purpose-verse reveal">
    <span class="eyebrow">Propósito</span>
    <h2>"Melhores são dois do que um."<span class="ref">Eclesiastes 4:9</span></h2>
    <p class="comp">Nosso amor nunca foi apenas sobre nós.<br>
    Sempre foi sobre caminhar juntos em direção ao propósito que Deus preparou para nossas vidas.<br><br>
    Que continuemos construindo, servindo, sonhando e amando, até que a eternidade revele tudo aquilo que Ele escreveu para nós.</p>
  </div>

  <div class="purpose-gallery reveal">
    <!-- PHOTO SLOTS: propósito / vida a dois -->
    <div class="ph" data-photo-slot="purpose-1">
      <img data-src="fotos/09.jpeg" alt="" loading="lazy">
      <div class="ph-label">FOTO 07<small>retrato, 3:4</small></div>
    </div>
    <div class="ph" data-photo-slot="purpose-2">
      <img data-src="fotos/11.jpeg" alt="" loading="lazy">
      <div class="ph-label">FOTO 08<small>retrato, 3:4</small></div>
    </div>
    <div class="ph" data-photo-slot="purpose-3">
      <img data-src="fotos/01.jpeg" alt="" loading="lazy">
      <div class="ph-label">FOTO 09<small>retrato, 3:4</small></div>
    </div>
  </div>
</section>

<!-- =========================================================
     SEÇÃO 6 — CONTADOR
========================================================= -->
<section class="section counter-section" id="counter">
  <div class="section-head reveal">
    <span class="eyebrow">Nosso tempo</span>
    <h2>Cada dia, uma nova escolha</h2>
  </div>

  <div class="counter-grid reveal">
    <div class="counter-item">
      <span class="counter-num" data-counter="years" data-target="7">0</span>
      <span class="counter-label">Anos juntos</span>
    </div>
    <div class="counter-item">
      <span class="counter-num" data-counter="months" data-target="0">0</span>
      <span class="counter-label">Meses</span>
    </div>
    <div class="counter-item">
      <span class="counter-num" data-counter="days" data-target="0">0</span>
      <span class="counter-label">Dias</span>
    </div>
  </div>

  <p class="counter-since reveal">desde 12 de junho de 2019</p>
</section>

<!-- =========================================================
     SEÇÃO 7 — DECLARAÇÃO FINAL
========================================================= -->
<section class="final" id="final">
  <!-- PHOTO SLOT: foto final em tela cheia, mesma proporção do hero -->
  <div class="ph zoom-slow" data-photo-slot="final-main">
    <img data-src="fotos/10.jpeg" alt="" loading="lazy">
    <div class="ph-label">FOTO FINAL — TELA CHEIA<small>recomendado 1920×1280 ou maior</small></div>
  </div>

  <div class="final-content reveal">
    <h2>7 anos escolhendo você.</h2>
    <p class="small">E continuarei escolhendo.<br>Até a eternidade.</p>
    <div class="final-symbol">
      <!-- aliança -->
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.3"><circle cx="12" cy="13" r="7"/><path d="M9 6.5 12 2l3 4.5" stroke-linejoin="round" stroke-linecap="round"/></svg>
      <span class="div"></span>
      <!-- cruz -->
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.3"><path d="M12 3v18M6 8h12" stroke-linecap="round"/></svg>
    </div>
  </div>
</section>

<!-- =========================================================
     RODAPÉ
========================================================= -->
<footer>
  <p class="verse">"Porque o amor jamais acaba."<span class="ref">1 Coríntios 13:8</span></p>
  <p class="names">Stéfano &amp; Amanda</p>
  <p class="made">7 anos · uma história escrita por Deus</p>
</footer>

<!-- =========================================================
     LIGHTBOX MODAL
========================================================= -->
<div class="lightbox" id="lightbox">
  <button class="lightbox-close" id="lightboxClose">Fechar ✕</button>
  <img src="" alt="" id="lightboxImg">
</div>

<script>
/* =========================================================
   1. PARTICLES — partículas luminosas discretas no hero
========================================================= */
(function initParticles(){
  const wrap = document.getElementById('particles');
  const COUNT = 22;
  for(let i=0;i<COUNT;i++){
    const p = document.createElement('span');
    const left = Math.random()*100;
    const size = 3 + Math.random()*5;
    const duration = 9 + Math.random()*10;
    const delay = Math.random()*-duration;
    p.style.left = left + '%';
    p.style.width = size + 'px';
    p.style.height = size + 'px';
    p.style.animationDuration = duration + 's';
    p.style.animationDelay = delay + 's';
    wrap.appendChild(p);
  }
})();

/* =========================================================
   2. SCROLL REVEAL — IntersectionObserver
========================================================= */
(function initReveal(){
  const items = document.querySelectorAll('.reveal, .reveal-zoom');
  const obs = new IntersectionObserver((entries)=>{
    entries.forEach(entry=>{
      if(entry.isIntersecting){
        entry.target.classList.add('is-visible');
        obs.unobserve(entry.target);
      }
    });
  }, { threshold:.18, rootMargin:'0px 0px -60px 0px' });
  items.forEach(el=>obs.observe(el));

  // hero text reveals immediately on load
  requestAnimationFrame(()=>{
    setTimeout(()=> document.getElementById('hero-text').classList.add('is-visible'), 250);
  });
})();

/* =========================================================
   3. NAV — aparece após sair do hero
========================================================= */
(function initNav(){
  const nav = document.getElementById('nav');
  const hero = document.getElementById('hero');
  const obs = new IntersectionObserver((entries)=>{
    entries.forEach(entry=>{
      nav.classList.toggle('visible', !entry.isIntersecting);
    });
  }, { threshold:0 });
  obs.observe(hero);
})();

/* =========================================================
   4. PHOTO LOADER
   -----------------------------------------------------------
   Para inserir as fotos depois: defina data-src="caminho.jpg"
   em cada <img>. Este script verifica se data-src está
   preenchido; se sim, carrega a imagem com fade-in suave e
   esconde o rótulo de placeholder. Se estiver vazio, mantém
   o placeholder visível normalmente.
========================================================= */
(function initPhotoLoader(){
  const imgs = document.querySelectorAll('.ph img[data-src]');
  imgs.forEach(img=>{
    const src = img.getAttribute('data-src');
    if(src && src.trim() !== ''){
      img.src = src;
      img.addEventListener('load', ()=>{
        img.classList.add('loaded');
        const label = img.parentElement.querySelector('.ph-label');
        if(label) label.style.display = 'none';
      });
    }
  });
})();

/* =========================================================
   5. LIGHTBOX — galeria cinematográfica
========================================================= */
(function initLightbox(){
  const lightbox = document.getElementById('lightbox');
  const lightboxImg = document.getElementById('lightboxImg');
  const closeBtn = document.getElementById('lightboxClose');

  document.querySelectorAll('[data-lightbox]').forEach(el=>{
    el.addEventListener('click', ()=>{
      const img = el.querySelector('img');
      const src = img.getAttribute('src');
      if(!src) return; // sem foto ainda, não abre
      lightboxImg.src = src;
      lightboxImg.alt = img.alt || '';
      lightbox.classList.add('open');
      document.body.style.overflow = 'hidden';
    });
  });

  function close(){
    lightbox.classList.remove('open');
    document.body.style.overflow = '';
  }
  closeBtn.addEventListener('click', close);
  lightbox.addEventListener('click', (e)=>{ if(e.target === lightbox) close(); });
  document.addEventListener('keydown', (e)=>{ if(e.key === 'Escape') close(); });
})();

/* =========================================================
   6. CONTADOR — dias / meses / anos juntos
   -----------------------------------------------------------
   Ajuste START_DATE para a data real de início do relacionamento.
========================================================= */
(function initCounter(){
  const START_DATE = new Date('2019-06-12T00:00:00');

  function diffParts(start, now){
    let years = now.getFullYear() - start.getFullYear();
    let months = now.getMonth() - start.getMonth();
    let days = now.getDate() - start.getDate();

    if(days < 0){
      months -= 1;
      const prevMonth = new Date(now.getFullYear(), now.getMonth(), 0);
      days += prevMonth.getDate();
    }
    if(months < 0){
      years -= 1;
      months += 12;
    }

    const totalDays = Math.floor((now - start) / (1000*60*60*24));
    return { years, months, days, totalDays };
  }

  const els = {
    years: document.querySelector('[data-counter="years"]'),
    months: document.querySelector('[data-counter="months"]'),
    days: document.querySelector('[data-counter="days"]')
  };

  const now = new Date();
  const parts = diffParts(START_DATE, now);

  // anima contagem suave até o valor real
  function animateTo(el, target, duration=1600){
    const startTime = performance.now();
    function tick(now){
      const progress = Math.min((now - startTime)/duration, 1);
      const eased = 1 - Math.pow(1-progress, 3);
      el.textContent = Math.round(eased * target);
      if(progress < 1) requestAnimationFrame(tick);
      else el.textContent = target;
    }
    requestAnimationFrame(tick);
  }

  const counterSection = document.getElementById('counter');
  const obs = new IntersectionObserver((entries)=>{
    entries.forEach(entry=>{
      if(entry.isIntersecting){
        animateTo(els.years, parts.years);
        animateTo(els.months, parts.months);
        animateTo(els.days, parts.totalDays % 30 === parts.days ? parts.days : parts.days);
        obs.unobserve(entry.target);
      }
    });
  }, { threshold:.4 });
  obs.observe(counterSection);
})();

/* =========================================================
   7. PARALLAX LEVE — hero e seção final
========================================================= */
(function initParallax(){
  const heroPh = document.querySelector('#hero .ph img');
  const finalPh = document.querySelector('#final .ph img');

  let ticking = false;
  function onScroll(){
    if(ticking) return;
    ticking = true;
    requestAnimationFrame(()=>{
      const y = window.scrollY;
      if(heroPh) heroPh.style.transform = `translateY(${y*0.18}px) scale(${heroPh.classList.contains('loaded') ? 1 : 1.08})`;
      const finalRect = document.getElementById('final').getBoundingClientRect();
      if(finalPh && finalRect.top < window.innerHeight && finalRect.bottom > 0){
        const offset = (window.innerHeight - finalRect.top) * 0.06;
        finalPh.style.transform = `translateY(${offset}px) scale(${finalPh.classList.contains('loaded') ? 1 : 1.08})`;
      }
      ticking = false;
    });
  }
  window.addEventListener('scroll', onScroll, { passive:true });
})();
</script>

</body>
</html>
